apiVersion: v1
kind: Service
metadata:
  name: logscope-dashboard
  namespace: logscope
spec:
  selector:
    app: logscope-dashboard
  ports:
    - name: http
      port: 8501
      targetPort: 8501
  type: ClusterIP
